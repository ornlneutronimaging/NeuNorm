name: NeuNorm
channels:
  - conda-forge
dependencies:
  # base
  - python
  - versioningit
  - toml
  # compute
  - astropy
  - numpy
  - scipy
  - ipython
  # visualization
  - pillow
  # utils
  - pathlib
  - tqdm
  - pre-commit
  # conda packaging
  - boa
  - conda-build
  - conda-verify
  - python-build
  # tests
  - pytest
  # doc
  - sphinx
  - sphinx_rtd_theme
  - nbsphinx
  # pip
  - pip
